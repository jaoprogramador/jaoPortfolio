let gql;_a01‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers],["default",()=>_a01‍.o]]);_a01‍.w("apollo-server",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
  extend type Query {
    """
    Returns the authenticated user.
    """
    me: User
  }
`;

       const resolvers = {
  Query: {
    me: (obj, args, { authService }) => {
      return authService.getUser();
    },
  },
};

_a01‍.d({
  typeDefs,
  resolvers,
});
