let gql;_618‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers],["default",()=>_618‍.o]]);_618‍.w("apollo-server",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
  type Review {
    id: ID!
    user: User!
    repository: Repository!
    userId: String!
    repositoryId: String!
    rating: Int!
    createdAt: DateTime!
    text: String
  }
`;

       const resolvers = {
  Review: {
    user: async ({ userId }, args, { dataLoaders: { userLoader } }) =>
      userLoader.load(userId),
    repository: (
      { repositoryId },
      args,
      { dataLoaders: { repositoryLoader } },
    ) => repositoryLoader.load(repositoryId),
  },
};

_618‍.d({
  typeDefs,
  resolvers,
});
