let gql;_e42‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers],["default",()=>_e42‍.o]]);_e42‍.w("apollo-server",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
  extend type Query {
    """
    Returns repository by an id.
    """
    repository(id: ID!): Repository
  }
`;

       const resolvers = {
  Query: {
    repository: async (obj, args, { dataLoaders: { repositoryLoader } }) =>
      repositoryLoader.load(args.id),
  },
};

_e42‍.d({
  typeDefs,
  resolvers,
});
